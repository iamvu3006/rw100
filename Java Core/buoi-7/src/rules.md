# Làm việc với DB
- Nếu dùng câu lệnh select thì phải trả về 1 List Entity, không trả về 1 Entity
- Nếu dùng câu lệnh insert, update, delete thì phải trả về 1 boolean để biết được có thành công hay không